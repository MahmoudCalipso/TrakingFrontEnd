import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.drivertracking.app',
  appName: 'Driver Tracking',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
